/**
 * Mock data has been extracted and migrated to MongoDB Atlas via backend seed script.
 * All frontend requests now fetch dynamically from the Express API endpoints (/api/v1).
 */
export const categories = [];
export const products = [];
